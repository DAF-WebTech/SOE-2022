"use strict";document.addEventListener("DOMContentLoaded",function(){const data=soefinding.findingJson.data.sort(function(a,b){return b["Tonnes per annum"]-a["Tonnes per annum"]});const series=data.map(d=>d["Tonnes per annum"]);const options1=soefinding.getDefaultPieChartOptions();options1.labels=data.map(d=>d["Emission type"]);options1.tooltip={y:{formatter:(val,options)=>{const percent=options.globals.seriesPercent[options.seriesIndex][0];return`${val.toLocaleString()} (${percent.toFixed(1)}%)`}}};options1.xaxis.categories=["Emission type","Tonnes per annum"];soefinding.state.chart1={options:options1,series,chartactive:true};new Vue({el:"#chartContainer",data:soefinding.state,computed:{heading1:()=>`Proportion of annual average vehicle emissions by type in 2010`},methods:{formatter1:val=>val.toLocaleString()}})});
//# sourceMappingURL=vehicle-emissions.js.map